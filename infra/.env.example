# Connection string for local development against the docker-compose Postgres.
# Copy to infra/.env (gitignored) or export DATABASE_URL yourself.
# Host port 55432 is mapped in docker-compose.yml to avoid clashing with a
# local Postgres already bound to 5432.
DATABASE_URL=postgres://election_pool:election_pool@localhost:55432/election_pool
